

def checkAuthPacket(packet_dict: dict):
    nas_auth,user_auth=False,False



    return 

def checkValidNas(packet_dict: dict):
    for keys in packet_dict.keys:
        if keys == "NAS-IP-ADDRESS":
            